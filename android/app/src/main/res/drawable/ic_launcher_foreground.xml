<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon foreground: the UDEN Deal wordmark inset so it stays
     inside Android's circular/squircle safe zone (logoo.png is full-bleed). -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_launcher_logo"
    android:inset="14%" />
